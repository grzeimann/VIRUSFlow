"""
This module has been removed in favor of the centralized YAML-driven QA engine (virusflow.qa.engine)
invoked via ArtifactService(...).diagnostics. It remains only as a stub to prevent accidental imports.
Any attempt to import or use this module will raise an ImportError with guidance.
"""
from __future__ import annotations

raise ImportError(
    "virusflow.qa.diagnostics is removed. Use ArtifactService(...).diagnostics (YAML-driven QA engine) instead."
)
